import { mount } from 'vue-test-utils';
import expect from 'expect';
import CardRegister from '../../resources/assets/js/views/Cards/CardRegister.vue';

describe ('CardRegister', () => {
    
    it('can add cards', () => {

      let wrapper = mount(CardRegister);    
       
       //let newCard = wrapper.find('.name');

      // newCard.element.value = 'Paul';
      // newCard.trigger('input');

       

       //wrapper.find('button').trigger('click');

       //expect(wrapper.find('ul').text()).toContain('Paul');



    });

});